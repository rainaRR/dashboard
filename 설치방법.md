# 휴대폰에서 보는 대시보드 만들기

컴퓨터를 꺼놔도 시세가 자동으로 갱신되는 웹페이지를 만듭니다.
깃허브라는 무료 서비스가 대신 일해주는 방식입니다. **돈은 한 푼도 들지 않습니다.**

처음 한 번만 설정하면 그 뒤로는 손댈 일이 없습니다. 15분쯤 걸립니다.

---

## 준비물

- 이메일 주소 하나
- 이 폴더 (`깃허브-대시보드`)

---

## 1단계 · 깃허브 계정 만들기 (3분)

이미 계정이 있으시면 2단계로 건너뛰세요.

1. 인터넷 주소창에 **github.com** 을 칩니다.
2. 오른쪽 위 **Sign up**(가입) 버튼을 누릅니다.
3. 이메일, 비밀번호, 아이디를 넣습니다.
   - **아이디는 나중에 웹주소가 됩니다.** 짧고 영어로 된 것이 좋습니다.
   - 예를 들어 아이디가 `raina`면 주소는 `raina.github.io/...`가 됩니다.
4. 이메일로 온 인증 번호를 넣으면 끝입니다.

---

## 2단계 · 저장소 만들기 (2분)

저장소는 파일을 담아두는 폴더라고 생각하시면 됩니다.

1. 로그인한 상태에서 오른쪽 위 **＋** 를 누르고 **New repository**(새 저장소)를 고릅니다.
2. 이렇게 채웁니다.

   | 칸 | 넣을 값 |
   |---|---|
   | Repository name | `dashboard` |
   | 공개 설정 | **Public**(공개)을 고릅니다 |
   | Add a README file | 체크하지 **않습니다** |

   > **왜 공개인가요?** 무료 계정은 공개일 때만 웹페이지를 만들 수 있습니다.
   > 올라가는 건 시세 숫자와 신문 요약뿐이라 걱정하실 내용은 없습니다.
   > 다만 이름·계좌·연락처 같은 개인 정보는 절대 넣지 마세요.

3. 맨 아래 **Create repository** 를 누릅니다.

---

## 3단계 · 파일 올리기 (3분)

1. 방금 만든 저장소 화면에서 **uploading an existing file** 이라는 파란 글씨를 누릅니다.
   - 안 보이면 **Add file → Upload files** 를 누르세요.
2. 컴퓨터에서 `깃허브-대시보드` 폴더를 엽니다.
3. **폴더 자체가 아니라, 폴더 안에 있는 것들을 전부** 끌어다 놓습니다.

   ```
   index.html          ← 화면
   fetch_data.py       ← 시세 긁어오는 코드
   data (폴더)         ← 숫자가 담기는 곳
   .github (폴더)      ← 자동 실행 설정
   설치방법.md          ← 이 문서
   ```

   > `.github` 폴더가 안 보이면 숨김 파일이라 그렇습니다.
   > 윈도우 탐색기에서 **보기 → 숨긴 항목** 에 체크하면 나타납니다.

4. 다 올라가면 아래 **Commit changes** 를 누릅니다.

---

## 4단계 · 웹페이지 켜기 (2분)

1. 저장소 위쪽 **Settings**(설정) 탭을 누릅니다.
2. 왼쪽 목록에서 **Pages** 를 누릅니다.
3. **Source** 를 `Deploy from a branch` 로 둡니다.
4. **Branch** 를 `main` / `/ (root)` 로 고르고 **Save** 를 누릅니다.
5. 1~2분 기다렸다 새로고침하면 위쪽에 주소가 나타납니다.

   ```
   https://아이디.github.io/dashboard/
   ```

   **이 주소가 손님의 대시보드입니다.** 어디서든 이 주소만 열면 됩니다.

---

## 5단계 · 자동 갱신 켜기 (2분)

1. 저장소 위쪽 **Actions** 탭을 누릅니다.
2. 초록색 **I understand my workflows, go ahead and enable them** 버튼이 보이면 누릅니다.
3. 왼쪽에 **시세 자동 갱신** 이라는 항목이 보입니다. 그걸 누릅니다.
4. 오른쪽 **Run workflow** 버튼을 눌러 지금 한 번 돌려봅니다.
5. 1~2분 뒤 초록색 체크 표시가 뜨면 성공입니다.

이제부터 **평일 오후 4시 10분**과 **아침 7시**에 저절로 돌아갑니다.
손님 컴퓨터는 꺼져 있어도 됩니다.

---

## 6단계 · 휴대폰 홈화면에 넣기 (1분)

**아이폰**
1. 사파리로 위 주소를 엽니다.
2. 아래 가운데 **공유** 버튼(⬆️)을 누릅니다.
3. **홈 화면에 추가** 를 고르고 **추가** 를 누릅니다.

**안드로이드**
1. 크롬으로 위 주소를 엽니다.
2. 오른쪽 위 점 세 개를 누릅니다.
3. **홈 화면에 추가** 를 고릅니다.

앱처럼 아이콘이 생기고, 누르면 바로 열립니다.

---

## 자주 생기는 문제

**주소를 열었는데 아무것도 안 나와요**
Pages가 준비되는 데 최대 10분 걸립니다. 조금 뒤 다시 열어보세요.

**숫자가 며칠째 그대로예요**
화면 위에 주황색 안내문이 뜹니다.
저장소의 **Actions** 탭에 들어가 빨간 X 표시가 있는지 보세요.
있으면 눌러서 나오는 글을 저에게 그대로 보여주시면 고쳐드립니다.

> 깃허브는 60일 동안 저장소에 아무 변화가 없으면 자동 실행을 멈춥니다.
> 그때는 Actions 탭에서 **Enable workflow** 를 한 번 눌러주면 다시 돕니다.

**신문 브리핑에 새 날짜가 안 보여요**
신문 브리핑은 제가 사진을 읽고 만드는 것이라 자동이 아닙니다.
새로 정리한 날에는 `data/briefs.json` 파일을 새것으로 바꿔 올리셔야 합니다.
저에게 "깃허브용 브리핑 파일 만들어줘"라고 하시면 파일을 드립니다.

**종목을 바꾸고 싶어요**
`fetch_data.py` 파일을 열면 위쪽에 종목 목록이 있습니다.
거기에 `"종목코드": "종목이름"` 형태로 넣거나 빼시면 됩니다.
저에게 말씀하셔도 고쳐드립니다.

---

## 무엇이 자동이고 무엇이 수동인가

| 항목 | 자동 | 설명 |
|---|---|---|
| 코스피·코스닥 지수 | 🟢 자동 | 평일 하루 두 번 |
| 미국 지수·공포지수 | 🟢 자동 | 평일 하루 두 번 |
| 종목 시세 25개 | 🟢 자동 | 평일 하루 두 번 |
| 신문 브리핑 | 🔴 수동 | 사진을 읽어야 하므로 제가 만들어 드립니다 |

---

## 알아두실 점

- 자동 실행이 예정 시각보다 5~15분 늦을 수 있습니다. 깃허브 사정입니다.
- 시세는 **장중 실시간이 아니라 전 거래일 종가 기준**입니다. 무료로 쓸 수 있는 범위입니다.
- 데이터는 `FinanceDataReader`와 `yfinance`라는 무료 라이브러리에서 가져옵니다. API 키가 필요 없습니다.
